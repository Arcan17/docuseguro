# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Vector store
chromadb==0.5.20

# LLM providers
anthropic==0.40.0
openai==1.57.0  # also used as Groq SDK (OpenAI-compatible)

# Embeddings (local ONNX — no API key required)
fastembed==0.4.2

# Document parsing
pypdf==5.1.0
python-docx==1.1.2

# NLP / PII (spaCy optional — enable with SPACY_ENABLED=true)
spacy==3.8.3

# Config
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-multipart==0.0.9

# Auth (Fase 1 — cuentas)
bcrypt==4.2.0
PyJWT==2.9.0
email-validator==2.2.0

# HTTP client (Telegram, CRM webhook)
httpx==0.27.2

# Logging
structlog==24.4.0

# Scheduling (PII token cleanup)
apscheduler==3.10.4

# Token counting
tiktoken==0.8.0

# Dev / test
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-env==1.1.3
respx==0.21.1
ruff==0.8.0
mypy==1.13.0
