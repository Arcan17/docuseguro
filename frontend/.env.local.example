# Base URL of the PrivRAG API. Defaults to the live Railway deployment if unset.
NEXT_PUBLIC_API_BASE=https://privrag-production.up.railway.app

# Only needed if the API has auth enabled (API_KEY set on the backend).
# WARNING: this is exposed client-side — use only for demo keys, never a real secret.
NEXT_PUBLIC_API_KEY=
